type ProductAttribute = {
    id: number,
    name: string,
    options: string[],
    position: number,
    slug: string,
    variation: boolean,
    visible: boolean,
}

export default ProductAttribute;